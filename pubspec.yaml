name: ecommerce_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get:
  dio:
  fluttertoast:
  flutter_rating_bar:
  drift:
  sqlite3_flutter_libs:
  path_provider:
  path:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  drift_dev:
  build_runner:

flutter:
  uses-material-design: true
  assets:
    - assets/  
